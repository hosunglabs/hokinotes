@functions
{   

    public static String LengthLimit(String value)
    {
        if (value.Length > 200)
            return value.Substring(0, 200) + "...";
        else
            return value;
    }

}